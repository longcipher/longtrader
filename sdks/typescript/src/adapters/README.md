# adapters

Thin by design: Connect-over-undici inside `session.ts`; mock adapter planned.
Mirrors `bin/longtrader-worker/src/adapters/`.
Concept names: `Session`, `TradingPort`, `sync_state`, `OverflowPolicy` 1:1.
