# adapters (canonical placeholder)

Maps to `src/adapters/` — thin Connect-over-undici inside `session.ts`; mock adapter planned.
Concept names `Session`/`TradingPort`/`syncState`/`OverflowPolicy` 1:1.
