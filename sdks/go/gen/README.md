# gen

Generated Go stubs — output of `buf generate` (`proto/buf.gen.yaml`, `paths=source_relative`).
Never hand-edit; regenerate via `just sdk-generate` or `buf generate --template proto/buf.gen.yaml`.
For fully local regeneration without remote plugins: `bash scripts/gen-proto.sh`.
