# strategies (canonical placeholder)

Maps to `longtrader_sdk/strategies/` + `examples/` — portable grid strategy in every language.
Concept names `Session`/`TradingPort`/`sync_state`/`OverflowPolicy` 1:1.
