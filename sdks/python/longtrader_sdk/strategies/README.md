# strategies

Portable strategies — the same grid ships in every language.
Mirrors `bin/longtrader-worker/src/strategies/` and `sdks/typescript/examples/`.
Concept names: `Session`, `TradingPort`, `sync_state`, `OverflowPolicy` 1:1.
