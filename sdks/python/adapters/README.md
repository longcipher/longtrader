# adapters (canonical placeholder)

Maps to `longtrader_sdk/adapters/` — thin Connect-over-http inside `session.py`; mock adapter planned.
Concept names `Session`/`TradingPort`/`sync_state`/`OverflowPolicy` 1:1 with Rust/TS/Go.
