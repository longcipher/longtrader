> **English** | [中文](README.zh.md)

# cross_depth_maker — Cross-Venue Depth Market Maker

Uses the hedge venue's order-book mid price as fair value and quotes ±spread limit orders on the primary venue.

Params: `primary`/`hedge`, `symbol`, `spread`, `qty`.
